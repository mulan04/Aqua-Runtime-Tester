apiVersion: v1
kind: Service
metadata:
  name: {{ include "aqua-runtime-tester.fullname" . }}
  labels:
    {{- include "aqua-runtime-tester.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aqua-runtime-tester.selectorLabels" . | nindent 4 }}
